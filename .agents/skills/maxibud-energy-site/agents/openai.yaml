interface:
  display_name: "MAXIBUD ENERGY Site"
  short_description: "Design and build the MAXIBUD ENERGY product website"
  default_prompt: "Use the repository instructions and this project skill to implement the requested MAXIBUD ENERGY website change. Apply the reusable Nuxt 4 production skill when available, preserve concept-product disclosures, and report exact validation results."

policy:
  allow_implicit_invocation: true
