export default defineAppConfig({
  ui: {
    colors: {
      primary: 'brand',
      secondary: 'zinc',
      neutral: 'zinc'
    },
    button: {
      slots: {
        base: 'font-semibold rounded-md'
      }
    }
  }
})
